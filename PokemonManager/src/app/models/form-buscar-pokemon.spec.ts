import { FormBuscarPokemon } from './form-buscar-pokemon';

describe('FormBuscarPokemon', () => {
  it('should create an instance', () => {
    expect(new FormBuscarPokemon()).toBeTruthy();
  });
});
